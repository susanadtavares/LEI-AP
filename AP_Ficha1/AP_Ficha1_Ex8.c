int main () {
    float lado1, lado1, base, altura, raio;
    char figura[10];



    printf("Qual a figura? ")
    scanf("%s", figura);

    if (figura == quadrado) {
        printf("Insira o")
    } else if (figura == retangulo) {

    } else if (figura == circulo){

    } else {
        printf ("A figura não é válida!");
    }
    return 0;
}