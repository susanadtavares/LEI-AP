#include <stdio.h>
#include <string.h>



int main (void)
{
    int a1 = 0, a2 = 0;
    char mens[50];
    strcpy(mens, "Qual o valor da primeira variável (a1): ");
    a1 = leituraInt(mens); //vai aceitar o valor de a1
    strcpy(mens, "Qual o valor da segunda variável (a2): ");
    a2 = leitura1(m); //vai aceitar o valor de a2
    char*m = mens;

    a2 = leituraChar(m); //passa o ponteiro para a primeira posição do mens.
}

int leituraInt(char[]){
    
}

void trocaErrado()